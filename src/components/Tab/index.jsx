import styled, { css } from "styled-components"
import theme from "../../theme"

const Tab = styled.ul`
  background-color: ${theme.background};
  padding: .5rem 24px;
  list-style: none;
  display: flex;
  cursor: pointer;
`

export default Tab
